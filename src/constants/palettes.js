import colors from './colors';

const palettes = {
    heading: {
        color: colors.headerText,
        fontSize: 20,
        textAlign: 'center'
    },
    text: {
        color: colors.primary,
        fontSize: 17,
        textAlign: 'center'
    }
};

export default palettes;

