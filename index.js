/**
 * @format
 * @lint-ignore-every XPLATJSCOPYRIGHT1
 */

import { AppRegistry } from 'react-native';
import Root from './src/Root.js';

AppRegistry.registerComponent('reactNativeStructure', () => Root);
